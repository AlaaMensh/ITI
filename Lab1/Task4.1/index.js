
var radius = parseInt(prompt("Enter your radius"));

function circleArea(rad){
    return Math.PI * rad * rad;
}

alert("your Total Area of Circle is =  " + circleArea(radius) );

var value = parseInt(prompt("Enter your Value"));

alert("sqrt root of  "+ value +" is  " + Math.sqrt(value));


var angle = parseInt(prompt("Enter your Agnle"));

alert("cos of   "+ angle +" is  " + Math.cos(angle));

